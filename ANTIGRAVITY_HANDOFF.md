# CLOOMY 작업 인계서

이 저장소는 React 19 + Vite 기반의 방 정리 앱입니다. 현재 개발 서버는 `http://127.0.0.1:5175/`에서 실행되어 있습니다.

```bash
npm install
npm run dev
npm run build
npm run lint
```

Gemini 물건 인식은 `.env`에 `VITE_GEMINI_API_KEY`가 있어야 합니다. 필요한 환경 변수 형식은 `.env.example`을 참고하세요.

## 현재 구현 상태

### 방 배치 화면

주요 파일은 `src/pages/RoomPage.jsx`, `src/pages/RoomPage.css`, `src/hooks/useRoom.js`입니다.

- SVG 아이소메트릭(3D 느낌) 방, 두 개의 벽, 방바닥, 우측 하단의 방문을 표시합니다.
- 가구: 서랍장, 책장, 책상, 침대, 옷장, 정리함.
- 가구는 0.25칸 그리드로 이동·크기 변경·스냅되며, 방 밖으로 나가지 않아야 합니다.
- 일반 가구끼리 면적이 겹치지 않으며, 벽이나 다른 가구의 변에 붙일 수 있습니다.
- 마우스 드래그와 방향키로 이동합니다. Shift 선택으로 여러 가구를 함께 움직입니다.
- 90도 회전은 충돌하거나 방을 벗어나면 가까운 유효 위치를 탐색해 회전해야 합니다. 침대에는 180도 방향 전환도 있습니다.
- 선택 가구에는 네 방향의 크기 조절 핸들이 있고, 회전 상태에서도 방향에 맞춰 동작해야 합니다.
- 가구를 쌓아 복합 가구로 만들 수 있습니다. 쌓일 때 더 큰 가구 크기에 맞추고, 쌓인 가구는 함께 이동·회전·크기 변경합니다. 분리 시에는 빈 그리드 위치를 찾아 서로 겹치지 않게 둡니다.
- 되돌리기/다시하기 아이콘과 Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z 또는 Ctrl/Cmd+Y를 지원합니다.
- 바닥 보관은 별도 가구가 아니라 방 중앙의 흰색 사각형을 클릭한 가상 보관 위치입니다.
- 방문은 일반 오브젝트 아래 레이어로 그려져 가구에 가려질 수 있습니다. 방문을 선택했을 때만 위치·열림 방향 옵션이 보입니다.

### 침대 표현

`RoomPage.jsx`의 `SoftBedLayers`가 침대 매트리스 위에 다음을 그립니다.

- 침대 폭의 약 절반인 직사각형 베개 하나
- 침대 양쪽 끝에 맞는 일반적인 직사각형 이불
- 회전 및 180도 방향 전환 시 침대와 같은 방향으로 돌아야 함

사용자는 과도하게 푹신하거나 흘러내리는 이불 표현보다 단정하고 입체적인 형태를 선호합니다.

### 가구 이름과 물건 위치 연결

방 배치 모드에서 실제 가구를 선택하면 이름 입력칸과 `이름 저장` 버튼이 표시됩니다.

- `renameSelected`는 가구 이름을 바꿀 때 기존 `가구 이름 · N번째 칸`에 저장된 물건을 새 이름의 같은 N번째 칸으로 함께 이동합니다.
- `useRoom`의 `locations`가 가구 이름과 칸 수에서 계산되므로, **내 물건 추가**와 스캔 후 위치 선택에도 새 이름이 자동 반영됩니다.
- 바닥 보관과 방문에는 이름 변경 옵션을 표시하지 않습니다.

## 카메라 및 물건 인식

주요 파일은 `src/components/Camera.jsx`, `src/hooks/useCamera.js`, `src/pages/ScanPage.jsx`, `src/services/gemini.js`입니다.

- 카메라는 우선 후면 카메라를 요청하고, `OverconstrainedError` 또는 `NotFoundError`일 때 일반 웹캠으로 자동 재시도합니다.
- 권한 거부, 카메라 없음, 다른 앱이 카메라 사용 중, 브라우저 미지원 오류를 구분해 보여줍니다.
- 오류 화면에 `카메라 다시 시도`와 사진 업로드가 있습니다.
- 개발 환경에서 카메라가 필요하면 브라우저 주소창의 사이트 권한에서 Camera를 허용해야 합니다. 로컬 환경에 실제 웹캠이 없거나 다른 앱이 점유 중이면 사진 업로드로 계속 진행할 수 있습니다.

## 저장 방식

- 방 배치: 브라우저 `localStorage`의 `cloomy_room`
- 방문 설정: 브라우저 `localStorage`의 `cloomy_door`
- 초기 데이터 또는 이전 저장 데이터가 겹치면 `useRoom.js`가 그리드에서 분리 배치합니다.
- 예전 `shoeCabinet`, `floorStorage`, `vanity` 저장 타입은 방에서 제외합니다.

## 최근 검증

- `npm run build`: 통과
- `npm run lint`: 실패는 없지만 기존 경고가 있습니다. `gemini.js`, `useItems.js`, `ChatAgent.jsx`의 미사용 변수와 `useRoom.js`, `RoomPage.jsx`의 `setState` effect 경고입니다.
- Vite 번들 크기 경고가 있습니다. 현재 기능에는 영향이 없습니다.

## 주의할 점

- 현재 워킹 트리에 커밋되지 않은 변경이 많습니다. 다른 작업을 덮어쓰거나 `git reset`하지 마세요.
- 기존 코드는 JavaScript/JSX이며, `RoomPage.jsx`의 일부 편집기 JSX가 한 줄로 길게 구성되어 있습니다. 동작 변경 시 컴포넌트를 분리해 가독성을 개선해도 됩니다.
- 사용자는 화면이 복잡해지는 것을 싫어합니다. 선택한 오브젝트에 필요한 옵션만 보여주고, 단순한 입체감과 따뜻한 색감은 유지하세요.

## Antigravity에 붙여 넣을 시작 프롬프트

```text
C:\bypp의 CLOOMY React/Vite 앱을 이어서 작업해줘. 먼저 ANTIGRAVITY_HANDOFF.md와 src/pages/RoomPage.jsx, src/hooks/useRoom.js, src/components/Camera.jsx, src/hooks/useCamera.js를 읽어 현재 구현과 제약을 파악해. 기존 미커밋 변경은 보존하고, 사용자 요청을 받을 때마다 npm run build로 검증해. 방 배치 UI는 선택한 가구에 필요한 옵션만 표시하고, 0.25칸 그리드·방 안 경계·가구 충돌 방지·가구 쌓임 규칙을 모든 새 가구에도 일관되게 적용해.
```
